# Project Specific Skills & Rules

## 核心架构

- 目标平台: RISC-V (SG2002)

## 强制约束

- 如果我要让你生成某些功能，你需要先运行然后调试，直到没有问题再停止

## 编译方法

```shell
make MYPLAT=axplat-riscv64-sg2002
```

## 调试技能

- 在调试后需要关闭设备
- 每次执行都需要是从关闭到打开的初始状态
- 上板测试统一用 `./.cursor/skills/sg2002-run/sg2002-run <binary>`（详见 `.cursor/skills/sg2002-run/SKILL.md`）

## 如何开启或关闭设备

### 上传并运行（推荐）

```shell
make MYPLAT=axplat-riscv64-sg2002
./.cursor/skills/sg2002-run/sg2002-run examples/helloworld/helloworld_sg2002.bin
```

### 手动步骤（仅调试脚本时使用）

将 examples/helloworld/helloworld_sg2002.bin 复制到 /srv/tftp/arceos.bin，上电后 TFTP 自动下载，串口 `/dev/ttyUSB0` @ 115200 查看输出。

### 开启设备

```shell
sg2002-ctl 1
```

### 关闭设备

```
sg2002-ctl 0
```

